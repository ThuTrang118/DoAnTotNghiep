{
  "feature": "string",
  "description": "string",
  "decision_summary": {
    "condition_count": 0,
    "action_count": 0,
    "full_combination_count": 0,
    "reduced_rule_count": 0
  },
  "conditions": [
    {
      "id": "C1",
      "name": "câu hỏi điều kiện atomic bằng tiếng Việt",
      "source_fields": ["TênField"],
      "values": ["Y", "N"],
      "meaning_when_y": "ý nghĩa khi điều kiện đúng",
      "meaning_when_n": "ý nghĩa khi điều kiện sai"
    }
  ],
  "actions": [
    {
      "id": "A1",
      "name": "tên hành động/kết quả",
      "expected": "kết quả mong đợi lấy từ Step 1 hoặc đặc tả"
    }
  ],
  "decision_rules": [
    {
      "id": "DT_001",
      "type": "happy_path",
      "condition_states": {
        "C1": "Y"
      },
      "action_refs": ["A1"],
      "expected": "kết quả mong đợi",
      "coverage_refs": ["1"],
      "reduction_note": "ghi chú rút gọn"
    }
  ]
}

RÀNG BUỘC CỨNG:
1. Chỉ output 1 JSON object.
2. Không markdown, không ```json, không giải thích.
3. Không copy placeholder trong schema mẫu.
4. feature, description, decision_summary, conditions, actions, decision_rules bắt buộc phải có.
5. conditions/actions/decision_rules không được rỗng.
6. condition id dạng C1, C2, C3...
7. action id dạng A1, A2, A3...
8. decision_rule id dạng DT_001, DT_002, DT_003...
9. type chỉ dùng: happy_path, single_fault, boundary_valid, business_rule.
10. condition_states phải chứa đủ tất cả condition id.
11. condition_states chỉ dùng Y, N, -.
12. action_refs phải tham chiếu action id có thật.
13. expected không rỗng và phải khớp expected của action được tham chiếu.
14. coverage_refs bắt buộc phải có trong mọi decision_rule.
15. coverage_refs không được rỗng.
16. coverage_refs chỉ được dùng id có thật từ Step 1.
17. Mỗi invalid coverage_item ở Step 1 phải xuất hiện trong ít nhất 1 decision_rule.coverage_refs.
18. Có đúng 1 happy_path.
19. Tất cả condition_states của happy_path phải là Y.
20. Không tạo condition tổng quát kiểu "tất cả dữ liệu hợp lệ", "tất cả điều kiện hợp lệ", "tất cả ràng buộc được đáp ứng".
21. Không gộp toàn bộ lỗi của một field thành "<field> hợp lệ?" nếu field đó có nhiều invalid coverage khác bản chất.
22. decision_summary.condition_count = số phần tử thật trong conditions.
23. decision_summary.action_count = số phần tử thật trong actions.
24. decision_summary.full_combination_count = 2^condition_count.
25. decision_summary.reduced_rule_count = số phần tử thật trong decision_rules.
26. Tất cả mô tả phải là tiếng Việt.

OUTPUT:
Chỉ JSON object.
Không text ngoài JSON.
