{
  "feature": "feature key",
  "description": "mô tả ngắn bộ test data cuối cùng",
  "items": [
    {
      "Testcase": "mã testcase",
      "<InputField1>": "giá trị input lấy từ representative_value của Step 1",
      "<InputField2>": "giá trị input lấy từ representative_value của Step 1",
      "Expected": "kết quả mong đợi lấy từ decision_rule.expected của Step 2"
    }
  ]
}

RÀNG BUỘC:

1. Chỉ output đúng 1 JSON object.

2. Top-level chỉ được có:
   * feature
   *description
   * items

3. Mỗi item chỉ được có:
   * Testcase
   * toàn bộ input fields
   * Expected

4. Không được:
   * thiếu field;
   * thừa field;
   * dùng placeholder;
   * dùng pseudo-value;
   * tạo testcase trùng;
   * tạo item trùng toàn bộ dữ liệu;
   * markdown;
   * giải thích ngoài JSON.

5. Expected:
   * phải khớp decision_rule.expected;
   * nếu expected là field name thì phải resolve thành giá trị thực tế.